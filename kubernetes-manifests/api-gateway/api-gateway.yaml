apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: api-gateway
    tier: backend
  name: api-gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-gateway
      tier: backend
  strategy: {}
  template:
    metadata:
      labels:
        app: api-gateway
        tier: backend
    spec:
      imagePullSecrets:
        - name: ghcrcred
      containers:
        - image: ghcr.io/liberty52/liberty52-api-gateway:dev-v-2023.10.30-223808
          name: liberty-gateway
          resources: {}

---

apiVersion: v1
kind: Service
metadata:
  labels:
    app: api-gateway
    tier: backend
  name: api-gateway
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080
  type: ClusterIP
  selector:
    app: api-gateway
    tier: backend
